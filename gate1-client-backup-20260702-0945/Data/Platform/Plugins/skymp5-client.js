// Skyrim RP local join disables stock SkyMP JS plugins so the RP client owns the local transport.
try { printConsole('[SkyrimRP] stock skymp5-client.js disabled for local RP transport ownership'); } catch (_) {}
